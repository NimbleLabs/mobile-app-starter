# Base URL for the Rails API the mobile app talks to.
# In dev with a Mac running Rails on the same machine: use your LAN IP
# (not localhost) so the simulator/device can reach it. e.g. http://192.168.1.42:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
