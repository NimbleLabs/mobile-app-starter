/**
 * App identity — the single place to rename when you fork this starter.
 *
 * Used by <BrandMark> (tile text), the web tab bar label, and the auth
 * screens' copy. Change these three strings and the UI follows.
 */
export const Branding = {
  appName: 'Starter',
  markText: 'ST',
  tagline: 'Your app, ready to build on.',
} as const;
